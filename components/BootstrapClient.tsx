'use client';

import { useEffect } from 'react';

export function BootstrapClient() {
  useEffect(() => {
    // Bootstrap JavaScript is already loaded via CDN in the layout
    // This component is just a placeholder for any client-side Bootstrap initialization
  }, []);

  return null;
}
